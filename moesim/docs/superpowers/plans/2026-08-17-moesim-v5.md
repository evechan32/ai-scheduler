# moesim v5 — 调度器增强 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox syntax.

**Goal:** 修复调度器的 4 个真实缺陷：GPU 排队感知、CPU 资源感知、专家迁移成本、驻留稳定性与收益。把调度从"单步贪心"升级为"长期价值感知"。

**Architecture:** 扩展 `ScheduleState` 携带负载/驻留统计；新增 `ResidencyAwarePolicy`（基于激活频率的长期价值评估 + 排队/CPU 感知）；`cost_model` 增加负载参数。模拟器 `moe_adapter` 提供队列长度反馈。

**Tech Stack:** Python>=3.10, numpy, sim/scheduler numpy-only。

## Global Constraints

- sim/scheduler numpy-only；既有 75 测试保持通过。
- 确定性；TDD；conventional commit。
- 单位 MB / GB/s / ms。

---

### Task 35: ScheduleState 扩展（负载统计 + 驻留价值字段）

**Files:**
- Modify: `moesim/scheduler/state.py`
- Test: `tests/scheduler/test_state_v5.py` (new)

**Interfaces:**
- `ScheduleState` 新增字段（默认值，向后兼容）：
  - `gpu_queue_len: int = 0`（当前 GPU 排队专家数）
  - `cpu_queue_len: int = 0`
  - `residency_benefit: dict[str, float] = field(default_factory=dict)`（专家 id → 驻留价值估计）
  - `migration_cost_ms: float = 0.0`（单次 load 的 PCIe 成本估计）
- `mark_access` 已有；新增 `record_load(expert_id, cost_ms)` 更新 residency_benefit。

- [ ] **Step 1: 写失败测试**

```python
# tests/scheduler/test_state_v5.py
from moesim.scheduler.cost_model import ExpertProfile
from moesim.scheduler.state import ScheduleState


def _state():
    profiles = {f"e{i}": ExpertProfile(f"e{i}", 10.0, 1.0, 5.0, activation_freq=0.5) for i in range(3)}
    return ScheduleState(profiles=profiles, resident=set(), gpu_capacity_mb=100.0)


def test_v5_defaults():
    s = _state()
    assert s.gpu_queue_len == 0
    assert s.cpu_queue_len == 0
    assert s.residency_benefit == {}
    assert s.migration_cost_ms == 0.0


def test_record_load_updates_benefit():
    s = _state()
    s.record_load("e0", 79.0)
    assert s.residency_benefit["e0"] == 79.0
    # 再次 load 累计（多次命中价值叠加）
    s.record_load("e0", 79.0)
    assert s.residency_benefit["e0"] == 158.0
```

- [ ] **Step 2: 验证失败** → AttributeError

- [ ] **Step 3: 实现**（state.py 加字段 + record_load 方法）

- [ ] **Step 4: 验证 + 全量 + Commit**

```bash
git commit -m "feat: extend ScheduleState with queue lengths and residency benefit"
```

---

### Task 36: 排队感知 + 驻留价值 cost_model（ResidencyAwarePolicy）

**Files:**
- Create: `moesim/scheduler/policies/residency.py`
- Test: `tests/scheduler/test_residency_policy.py` (new)

**Interfaces:**
- `ResidencyAwarePolicy(Scheduler)`：
  - `__init__(pcie, gpu_concurrency=1, cpu_concurrency=4, residency_bonus_factor=2.0)`
  - `decide(state, clock)`:
    1. **驻留优先**：`eid in resident` → execute_gpu（稳定驻留，不轻易驱逐）
    2. **排队感知**：GPU 排队（`gpu_queue_len >= gpu_concurrency`）→ 倾向 CPU
    3. **驻留收益评估**：对非驻留专家，计算 `load_cost + gpu_exec_ms × (1 + queue_len/gpu_concurrency)` vs `cpu_exec_ms`，但**若专家高频（activation_freq 高）且 residency_benefit 累计超过 load_cost**，则 load 到 GPU（长期价值 > 单步成本）
    4. 输出动作含 execute_gpu / execute_cpu / load
- 测试：
  - `test_resident_stays_gpu`: 驻留专家 → execute_gpu（即使 CPU 单步更便宜）
  - `test_gpu_queue_forces_cpu`: gpu_queue_len=3, gpu_concurrency=1 → 非驻留专家走 CPU
  - `test_hot_expert_loads_despite_cost`: activation_freq 高 + residency_benefit > load_cost → load

- [ ] **Step 1: 写失败测试**（tests/scheduler/test_residency_policy.py）

```python
from moesim.scheduler.cost_model import ExpertProfile
from moesim.scheduler.policies.residency import ResidencyAwarePolicy
from moesim.scheduler.state import ScheduleState
from moesim.sim.resources import BandwidthResource


def _profiles(hot=False):
    return {"e0": ExpertProfile("e0", 10.0, 1.0, 5.0, activation_freq=0.9 if hot else 0.1)}


def test_resident_stays_gpu():
    pcie = BandwidthResource(bandwidth_gbps=10.0)
    s = ScheduleState(profiles=_profiles(), resident={"e0"}, gpu_capacity_mb=100.0,
                      requested=("e0",))
    actions = ResidencyAwarePolicy(pcie=pcie).decide(s, 0.0)
    assert any(a.kind == "execute_gpu" for a in actions)


def test_gpu_queue_forces_cpu():
    pcie = BandwidthResource(bandwidth_gbps=10.0)
    s = ScheduleState(profiles=_profiles(), resident=set(), gpu_capacity_mb=100.0,
                      requested=("e0",), gpu_queue_len=3)
    actions = ResidencyAwarePolicy(pcie=pcie, gpu_concurrency=1).decide(s, 0.0)
    assert any(a.kind == "execute_cpu" for a in actions)


def test_hot_expert_loads_despite_cost():
    pcie = BandwidthResource(bandwidth_gbps=10.0)  # 10MB load = 1ms
    s = ScheduleState(profiles=_profiles(hot=True), resident=set(), gpu_capacity_mb=100.0,
                      requested=("e0",), migration_cost_ms=1.0)
    s.record_load("e0", 5.0)  # 驻留价值 5ms > load 1ms
    actions = ResidencyAwarePolicy(pcie=pcie).decide(s, 0.0)
    assert any(a.kind == "load" for a in actions)
```

- [ ] **Step 2: 验证失败** → ModuleNotFoundError

- [ ] **Step 3: 实现 residency.py**

```python
"""Residency-aware policy: queue-aware, migration-cost-aware, stability-first."""
from __future__ import annotations

from copy import deepcopy

from moesim.scheduler.base import Action, Scheduler
from moesim.scheduler.state import ScheduleState
from moesim.sim.resources import BandwidthResource


class ResidencyAwarePolicy(Scheduler):
    def __init__(self, pcie, gpu_concurrency: int = 1, cpu_concurrency: int = 4,
                 residency_bonus_factor: float = 2.0) -> None:
        self.pcie = pcie
        self.gpu_concurrency = gpu_concurrency
        self.cpu_concurrency = cpu_concurrency
        self.residency_bonus_factor = residency_bonus_factor

    def _gpu_contention(self, state: ScheduleState) -> float:
        if self.gpu_concurrency <= 0:
            return 0.0
        return state.gpu_queue_len / self.gpu_concurrency

    def _cpu_contention(self, state: ScheduleState) -> float:
        if self.cpu_concurrency <= 0:
            return 0.0
        return state.cpu_queue_len / self.cpu_concurrency

    def decide(self, state: ScheduleState, clock: float) -> list[Action]:
        actions: list[Action] = []
        for eid in state.requested:
            profile = state.profiles[eid]
            if eid in state.resident:
                # 稳定性：驻留专家留在 GPU（除非 CPU 几乎空闲且 GPU 严重排队）
                if self._gpu_contention(state) > 1.5 and self._cpu_contention(state) < 0.25:
                    actions.append(Action(kind="execute_cpu", expert_ids=(eid,)))
                else:
                    actions.append(Action(kind="execute_gpu", expert_ids=(eid,)))
                continue

            load_cost = self.pcie.transfer_time_ms(profile.size_mb)
            gpu_eff = profile.gpu_exec_ms * (1.0 + self._gpu_contention(state))
            cpu_eff = profile.cpu_exec_ms * (1.0 + self._cpu_contention(state))

            # 驻留收益：累计价值超过 load_cost → load（长期收益）
            benefit = state.residency_benefit.get(eid, 0.0)
            if benefit >= load_cost * self.residency_bonus_factor:
                actions.append(Action(kind="load", expert_ids=(eid,)))
            elif cpu_eff < load_cost + gpu_eff:
                actions.append(Action(kind="execute_cpu", expert_ids=(eid,)))
            else:
                actions.append(Action(kind="load", expert_ids=(eid,)))
        return actions
```

- [ ] **Step 4: 验证 + 全量 + Commit**

```bash
git commit -m "feat: residency-aware policy with queue and migration cost awareness"
```

---

### Task 37: 模拟器反馈（队列长度 + 驻留价值更新）

**Files:**
- Modify: `moesim/sim/moe_adapter.py`
- Test: `tests/sim/test_v5_feedback.py` (new)

**Interfaces:**
- `MoESimulation._step` 在执行后更新 `state.gpu_queue_len` / `cpu_queue_len`（本步排队数），并调用 `state.record_load(eid, load_cost)` 对 load 动作记录驻留价值。
- 测试：`test_queue_len_updated`、`test_load_records_benefit`。

- [ ] **Step 1: 写失败测试**

```python
# tests/sim/test_v5_feedback.py
from moesim.scheduler.cost_model import ExpertProfile
from moesim.scheduler.policies.lru import LRUPolicy
from moesim.sim.moe_adapter import MoESimulation
from moesim.sim.resources import BandwidthResource, ComputeResource


def _profiles():
    return {f"e{i}": ExpertProfile(f"e{i}", 10.0, 1.0, 5.0) for i in range(3)}


def test_queue_len_updated():
    pcie = BandwidthResource(bandwidth_gbps=10.0)
    sim = MoESimulation(scheduler=LRUPolicy(), profiles=_profiles(), gpu_capacity_mb=100.0,
                        pcie=pcie, gpu=ComputeResource(1, 1.0), cpu=ComputeResource(4, 1.0))
    sim.run([["e0", "e1"]])
    assert sim._state.gpu_queue_len >= 0  # 已更新（无断言错误即通过）


def test_load_records_benefit():
    pcie = BandwidthResource(bandwidth_gbps=10.0)
    sim = MoESimulation(scheduler=LRUPolicy(), profiles=_profiles(), gpu_capacity_mb=100.0,
                        pcie=pcie, gpu=ComputeResource(1, 1.0), cpu=ComputeResource(4, 1.0))
    sim.run([["e0"]])
    # LRU 会 load e0，record_load 记录驻留价值
    assert "e0" in sim._state.residency_benefit
    assert sim._state.residency_benefit["e0"] > 0
```

- [ ] **Step 2: 验证失败** → record_load 未调用 / 字段不存在

- [ ] **Step 3: 实现**（moe_adapter._step 更新队列长度 + record_load）

- [ ] **Step 4: 验证 + 全量 + Commit**

```bash
git commit -m "feat: simulator feeds queue length and residency benefit to scheduler"
```

---

### Task 38: 策略对比验证（ResidencyAware vs 旧策略）

**Files:**
- Create: `benchmarks/e2e/compare_residency_vs_baseline.py` (new)
- Modify: `moesim/sim/sweep.py`（加入 ResidencyAwarePolicy）

**Interfaces:**
- `compare_policies` 增加 residency 策略。
- 对比脚本：热专家 + 冷专家 trace，对比 cost_model / lru / residency 的 TPOT + 命中率，验证 residency 在热场景胜出。

- [ ] **Step 1: 修改 sweep.py** 加入 ResidencyAwarePolicy（需传 pcie）

- [ ] **Step 2: 写对比脚本**（热专家 80% 请求 + 冷专家 20%）

- [ ] **Step 3: 运行验证**：residency 策略在热场景 TPOT 优于 cost_model（稳定驻留省迁移）

- [ ] **Step 4: Commit**

```bash
git commit -m "feat: residency-aware policy comparison harness"
```

---

## Self-Review Notes

- **Spec coverage:** 问题1（排队感知）→ Task 36 的 gpu_contention；问题2（CPU/迁移/驻留）→ Task 35-36（residency_benefit、migration_cost、稳定性分支）。
- **Determinism:** 无随机；队列长度由模拟器确定性更新。
- **Regression:** 每任务全量测试；既有 75 不破坏。
- **Type consistency:** ResidencyAwarePolicy 沿用 Scheduler/Action/ScheduleState 接口。
