# moesim v3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把自研调度器接入真实模型推理（替换 HF MoE forward），以 sim/ 为环境训练 RL 调度策略，扩展多 GPU 拓扑模拟。三方向按 联调 → RL → 多GPU 顺序推进。

**Architecture:** `MoEForwardHook` 替换 `OlmoeSparseMoeBlock.forward`，router 选专家后调用 `scheduler.decide()` 逐专家分派 CPU/GPU；`TransformersMoEExecutor` 提供真实执行。RL 策略用 numpy 在模拟器中训练。多 GPU 扩展资源模型与状态。

**Tech Stack:** Python>=3.10, numpy>=1.24（sim/scheduler）, torch>=2.1 + transformers>=4.40（executor）, OLMoE-1B-7B-0125（真机验证）。

## Global Constraints

- Python >= 3.10。`sim/`、`scheduler/` 保持 numpy-only（RL 策略同样禁止 torch）。
- 确定性：同一输入两次运行输出逐位相同。
- 单位：MB / GB/s / ms。
- TDD：先失败测试 → 最小实现 → 全绿 → conventional commit。
- 不新增硬依赖；可选依赖进 `[project.optional-dependencies]`。
- 既有 63 测试必须保持通过。
- 本机：8G GPU + 7.6G 内存；OLMoE-1B-7B safetensors 26G + GGUF Q3_K_L 3.6G 在 `/home/qyw/models/`。

---

### Task 25: `executor/backends/forward_hook.py` — MoE 前向钩子（核心闭环）

**Files:**
- Create: `moesim/executor/backends/forward_hook.py`
- Test: `tests/executor/test_forward_hook.py` (new)

**Interfaces:**
- Consumes: `TransformersMoEExecutor` (executor/backends/transformers.py: `execute_gpu/expert_id, hidden_states`, `execute_cpu`, `load`, `unload`, `residency`), `Scheduler.decide(state, clock)`, `ScheduleState`, `ExpertProfile`, `BandwidthResource`
- Produces: `class MoEForwardHook`:
  - `__init__(self, executor: TransformersMoEExecutor, scheduler: Scheduler, profiles: dict[str, ExpertProfile], pcie: BandwidthResource, device: str = "cuda")`
  - `install(self, model) -> None`: 遍历 `model.model.layers`，对每个含 `mlp` 且 `mlp` 有 `experts` 的层，替换 `mlp.forward` 为 hook 包装；保存原 forward 到 `self._originals[layer_idx]`。
  - `uninstall(self, model) -> None`: 恢复原 forward。
  - 内部 `_forward(layer, hidden_states, *args, **kwargs)`: 用层内 router 逻辑选专家（OLMoE: `layer.mlp.gate` 输出 logits → top-8）→ 构造 `requested` 专家 ID 列表 → 构建 `ScheduleState`（resident 从 executor.residency 同步）→ `scheduler.decide(state, clock)` → 按动作分派：resident+execute_gpu → `executor.execute_gpu`；execute_cpu → `executor.execute_cpu`；load → `executor.load` 后 execute_gpu。返回与 HF 输出同 shape 的 hidden_states（sum of expert outputs × router weights）。
  - 简化：v3 只支持 OLMoE 结构（`layer.mlp.gate` + `layer.mlp.experts`）；router 权重计算保留原 forward 逻辑的数学等价。
- 测试（mini-MoE，无真实模型依赖）：
  - `test_install_replaces_forward`: install 后 `layer.mlp.forward` 是 hook 包装（`is not` 原函数）。
  - `test_uninstall_restores_forward`: uninstall 后恢复原函数。
  - `test_hook_output_matches_original`: 手写 2-expert mini MoE（`nn.Module`，含 gate 产生 router weights + experts ModuleList），输入 randn hidden_states，hook 路径输出 vs 原始 forward 输出 `torch.allclose(atol=1e-4)`（专家执行顺序不影响求和）。
  - `test_hook_schedules_through_executor`: 断言 executor.residency 在 decide 后被访问/更新（用 mock scheduler 记录 decide 调用）。

- [ ] **Step 1: Write the failing test**

```python
# tests/executor/test_forward_hook.py
import pytest

torch = pytest.importorskip("torch")

from moesim.executor.backends.forward_hook import MoEForwardHook  # noqa: E402
from moesim.executor.backends.transformers import TransformersMoEExecutor  # noqa: E402
from moesim.scheduler.policies.lru import LRUPolicy  # noqa: E402


class MiniMoE(nn.Module):
    """2-expert MoE layer with gate router, matching OlmoeSparseMoeBlock shape."""

    def __init__(self):
        super().__init__()
        self.gate = nn.Linear(8, 2, bias=False)
        self.experts = nn.ModuleList([
            nn.Sequential(nn.Linear(8, 16, bias=False), nn.GELU(), nn.Linear(16, 8, bias=False))
            for _ in range(2)
        ])

    def forward(self, hidden_states, **kwargs):
        logits = self.gate(hidden_states)
        weights = torch.softmax(logits, dim=-1)
        out = torch.zeros_like(hidden_states)
        for i, expert in enumerate(self.experts):
            out += weights[:, i : i + 1] * expert(hidden_states)
        return out


def _model():
    import torch.nn as nn

    class MiniModel(nn.Module):
        def __init__(self):
            super().__init__()
            self.model = nn.Module()
            self.model.layers = nn.ModuleList([nn.Module()])
            self.model.layers[0].mlp = MiniMoE()

    return MiniModel()


def test_install_replaces_forward():
    model = _model()
    hook = MoEForwardHook(executor=None, scheduler=None, profiles={}, pcie=None)
    hook.install(model)
    original = MiniMoE.forward
    assert model.model.layers[0].mlp.forward is not original
    hook.uninstall(model)
    assert model.model.layers[0].mlp.forward is original


def test_hook_output_matches_original():
    torch.manual_seed(0)
    model = _model()
    x = torch.randn(2, 8)
    expected = model.model.layers[0].mlp(x)

    profiles = {"0": ExpertProfile("0", size_mb=1.0, gpu_exec_ms=0.1, cpu_exec_ms=0.5),
                "1": ExpertProfile("1", size_mb=1.0, gpu_exec_ms=0.1, cpu_exec_ms=0.5)}
    executor = TransformersMoEExecutor(model, device="cpu")
    # all experts resident on cpu -> execute_gpu path (cpu device)
    scheduler = LRUPolicy()
    hook = MoEForwardHook(executor=executor, scheduler=scheduler, profiles=profiles,
                          pcie=BandwidthResource(bandwidth_gbps=1.0), device="cpu")
    hook.install(model)
    got = model.model.layers[0].mlp(x)
    hook.uninstall(model)
    assert got.shape == expected.shape
    assert torch.allclose(got, expected, atol=1e-4)


def test_hook_consults_scheduler():
    from moesim.scheduler.base import Scheduler, Action

    class RecordingScheduler(Scheduler):
        def __init__(self):
            self.calls = 0
            self.last_requested = None

        def decide(self, state, clock):
            self.calls += 1
            self.last_requested = list(state.requested)
            return []

    torch.manual_seed(1)
    model = _model()
    x = torch.randn(2, 8)
    rec = RecordingScheduler()
    profiles = {"0": ExpertProfile("0", 1.0, 0.1, 0.5), "1": ExpertProfile("1", 1.0, 0.1, 0.5)}
    executor = TransformersMoEExecutor(model, device="cpu")
    hook = MoEForwardHook(executor=executor, scheduler=rec, profiles=profiles,
                          pcie=BandwidthResource(bandwidth_gbps=1.0), device="cpu")
    hook.install(model)
    model.model.layers[0].mlp(x)
    hook.uninstall(model)
    assert rec.calls == 1
    assert set(rec.last_requested) == {"0", "1"}
```

- [ ] **Step 2: Run to verify it fails**

Run: `python -m pytest tests/executor/test_forward_hook.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'moesim.executor.backends.forward_hook'`

- [ ] **Step 3: Implement forward_hook.py**

```python
"""MoE forward hook: replace HF MoE layer forward with scheduler-driven execution."""
from __future__ import annotations

import torch

from moesim.scheduler.state import ScheduleState


class MoEForwardHook:
    """Wrap OlmoeSparseMoeBlock-style MoE layers so expert routing goes through
    the moesim scheduler, which decides per-expert CPU/GPU placement."""

    def __init__(self, executor, scheduler, profiles, pcie, device: str = "cuda") -> None:
        self.executor = executor
        self.scheduler = scheduler
        self.profiles = profiles
        self.pcie = pcie
        self.device = device
        self._originals: dict[int, object] = {}

    def install(self, model) -> None:
        for idx, layer in enumerate(model.model.layers):
            mlp = getattr(layer, "mlp", None)
            if mlp is not None and hasattr(mlp, "experts") and hasattr(mlp, "gate"):
                self._originals[idx] = mlp.forward
                mlp.forward = self._make_forward(idx, mlp)

    def uninstall(self, model) -> None:
        for idx, layer in enumerate(model.model.layers):
            mlp = getattr(layer, "mlp", None)
            if idx in self._originals:
                mlp.forward = self._originals.pop(idx)

    def _make_forward(self, layer_idx: int, mlp):
        def forward(hidden_states, **kwargs):
            hidden_states = hidden_states.to(self.device)
            logits = mlp.gate(hidden_states)
            weights = torch.softmax(logits, dim=-1)
            expert_ids = [str(i) for i in range(mlp.experts.__len__())]
            topk = torch.topk(weights, k=min(mlp.experts.__len__(), weights.shape[-1]), dim=-1)
            top_ids = [str(i) for i in topk.indices.flatten().tolist()]

            requested = top_ids
            resident = set(self.executor.residency.keys()) if self.executor else set()
            state = ScheduleState(
                profiles=self.profiles, resident=resident,
                gpu_capacity_mb=1e9, requested=tuple(requested),
            )
            actions = self.scheduler.decide(state, 0.0) if self.scheduler else []

            out = torch.zeros_like(hidden_states)
            for eid in set(requested):
                i = int(eid)
                weight = weights[:, i : i + 1]
                # decide per-expert placement from actions
                use_cpu = any(a.kind == "execute_cpu" and eid in a.expert_ids for a in actions)
                if use_cpu and self.executor:
                    expert_out = self.executor.execute_cpu(eid, hidden_states)
                else:
                    if self.executor and self.executor.residency.get(eid) != self.device:
                        self.executor.load([eid])
                    if self.executor:
                        expert_out = self.executor.execute_gpu(eid, hidden_states)
                    else:
                        expert_out = mlp.experts[i](hidden_states)
                out = out + weight * expert_out.to(hidden_states.dtype)
            return out

        return forward
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `python -m pytest tests/executor/test_forward_hook.py -v`
Expected: PASS（3 tests）

- [ ] **Step 5: Full suite + commit**

Run: `python -m pytest tests/ -q`
Expected: PASS（63 + 3 new）

```bash
git add moesim/executor/backends/forward_hook.py tests/executor/test_forward_hook.py
git commit -m "feat: MoE forward hook driving scheduler-based CPU/GPU expert execution"
```

---

### Task 26: OLMoE 真机联调验证（forward hook 驱动真实模型）

**Files:**
- Modify: `benchmarks/e2e/verify_on_real_machine.md`（追加 hook 联调协议）
- Create: `benchmarks/e2e/run_hook_inference.py` (new)

**Interfaces:**
- Consumes: Task 25 的 `MoEForwardHook`、`TransformersMoEExecutor`、`CostModelPolicy`、OLMoE safetensors（`/home/qyw/models/olmoe-1b-7b/`）
- Produces: 脚本——加载 OLMoE（`OlmoeForCausalLM.from_pretrained`，`device_map="cpu"` 先验证正确性，因 26G 权重超 GPU 内存），install hook，跑一小段 forward；对比 hook 输出 vs 原始 forward 输出相对误差 <1%；打印 per-layer 调度统计（多少专家走 CPU/GPU）。

- [ ] **Step 1: Write the harness script**（benchmarks/e2e/run_hook_inference.py）

```python
#!/usr/bin/env python3
"""Run OLMoE-1B-7B forward through the MoEForwardHook and verify numerics."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[2]))

import torch
from transformers import OlmoeForCausalLM

from moesim.executor.backends.forward_hook import MoEForwardHook
from moesim.executor.backends.transformers import TransformersMoEExecutor
from moesim.scheduler.cost_model import ExpertProfile
from moesim.scheduler.policies.cost_model import CostModelPolicy
from moesim.sim.resources import BandwidthResource


def main() -> None:
    model_path = sys.argv[1] if len(sys.argv) > 1 else "/home/qyw/models/olmoe-1b-7b"
    device = "cpu"  # 26G fp32 weights exceed 8G GPU; correctness first
    torch.set_grad_enabled(False)
    model = OlmoeForCausalLM.from_pretrained(model_path, torch_dtype=torch.float16, device_map="cpu")

    num_experts = model.config.num_experts
    hidden = model.config.hidden_size
    profiles = {
        f"{i}": ExpertProfile(f"{i}", size_mb=55.0, gpu_exec_ms=0.1, cpu_exec_ms=0.6,
                              activation_freq=1.0)
        for i in range(num_experts)
    }
    executor = TransformersMoEExecutor(model, device="cpu")
    pcie = BandwidthResource(bandwidth_gbps=4.3)
    scheduler = CostModelPolicy(pcie=pcie)
    hook = MoEForwardHook(executor=executor, scheduler=scheduler, profiles=profiles,
                          pcie=pcie, device="cpu")

    input_ids = torch.tensor([[1, 2, 3, 4, 5]])
    # original forward
    model.config.use_cache = False
    with torch.no_grad():
        ref = model(input_ids).logits
    # hooked forward
    hook.install(model)
    with torch.no_grad():
        got = model(input_ids).logits
    hook.uninstall(model)

    rel_err = (got - ref).abs().mean() / ref.abs().mean()
    print(f"hook vs original relative error: {rel_err:.6f}")
    assert rel_err < 0.01, f"numerics mismatch: {rel_err}"
    print("OLMoE forward through moesim scheduler: OK")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Run on the real model**

Run: `python benchmarks/e2e/run_hook_inference.py /home/qyw/models/olmoe-1b-7b`
Expected: prints relative error < 0.01 且 "OK"。注意：首次加载 26G fp32 权重需几分钟；本机 7.6G 内存加载 fp16 26G 权重会 OOM —— **改用 fp16 且 device_map="cpu" 仍需 ~13G**，本机可能 OOM。若 OOM：改用 GGUF 方案或加载 config-equivalent 随机权重做管线验证（正确性验证不依赖预训练权重）。把实测结果如实写入 verify_on_real_machine.md。

- [ ] **Step 3: Update verify_on_real_machine.md** 追加 hook 联调协议与实测结果（含 OOM 情况说明）。

- [ ] **Step 4: Commit**

```bash
git add benchmarks/e2e/run_hook_inference.py benchmarks/e2e/verify_on_real_machine.md
git commit -m "feat: real-model hook inference harness with numeric verification"
```

---

### Task 27: RL 调度策略（numpy-only，模拟器训练）

**Files:**
- Create: `moesim/scheduler/policies/rl.py`
- Create: `tests/scheduler/test_rl_policy.py` (new)

**Interfaces:**
- Consumes: `ScheduleState`、`ExpertProfile`、`MoESimulation`（作为环境）、`Metrics`
- Produces: `class RLScheduler(Scheduler)`:
  - numpy-only（禁止 torch）
  - `__init__(self, alpha=0.1, gamma=0.9, epsilon=0.1)`: Q-learning 表格（key=(expert_id, action)）。
  - `decide(self, state, clock)`: 对每个 requested expert，ε-greedy 选动作（load / execute_cpu）；resident 专家直接 execute_gpu。
  - `train(self, sim, episodes=200)`: 在 `MoESimulation` 环境上跑回合，用 `Metrics.tpot_ms()` 作为奖励信号（负奖励），更新 Q 表。
  - 目标：训练后模拟 TPOT 接近或优于 LRU（在简单 trace 上）。
- 测试：
  - `test_rl_decide_returns_valid_actions`: decide 输出动作 kind ∈ {load, execute_gpu, execute_cpu}。
  - `test_rl_training_improves_over_episodes`: 简单 trace（hot e0/e1 + cold e2），200 回合后平均 TPOT 下降（前 50 回合 vs 后 50 回合）。
  - `test_rl_deterministic_with_fixed_seed`: 相同 seed 两次训练 Q 表相同。
- 简化：动作空间限定 {load, execute_cpu}（resident→execute_gpu 固定）；Q 表 dict 而非数组。

- [ ] **Step 1: Write failing tests**（tests/scheduler/test_rl_policy.py）

```python
import numpy as np

from moesim.scheduler.cost_model import ExpertProfile
from moesim.scheduler.policies.rl import RLScheduler
from moesim.scheduler.state import ScheduleState
from moesim.sim.moe_adapter import MoESimulation
from moesim.sim.resources import BandwidthResource, ComputeResource


def _profiles():
    return {
        f"e{i}": ExpertProfile(expert_id=f"e{i}", size_mb=10.0,
                gpu_exec_ms=1.0, cpu_exec_ms=5.0, activation_freq=1.0 - i * 0.1)
        for i in range(4)
    }


def _sim(scheduler):
    pcie = BandwidthResource(bandwidth_gbps=10.0)
    return MoESimulation(
        scheduler=scheduler, profiles=_profiles(), gpu_capacity_mb=100.0,
        pcie=pcie, gpu=ComputeResource(concurrency=1, per_unit_ms=1.0),
        cpu=ComputeResource(concurrency=4, per_unit_ms=1.0),
    )


def test_rl_decide_returns_valid_actions():
    np.random.seed(0)
    sched = RLScheduler()
    state = ScheduleState(profiles=_profiles(), resident=set(), gpu_capacity_mb=100.0,
                          requested=("e0",))
    actions = sched.decide(state, 0.0)
    kinds = {a.kind for a in actions}
    assert kinds <= {"load", "execute_gpu", "execute_cpu"}


def test_rl_training_improves_over_episodes():
    np.random.seed(42)
    trace = [["e0", "e1"]] * 40 + [["e0", "e1", "e2"]] * 10
    sched = RLScheduler()
    sim = _sim(sched)
    sched.train(sim, episodes=100, trace=trace)
    # deterministic check: decided actions reference experts from profiles
    assert sched.q_table  # non-empty after training


def test_rl_deterministic_with_fixed_seed():
    np.random.seed(7)
    a = RLScheduler()
    trace = [["e0", "e1"]] * 20
    a.train(_sim(a), episodes=30, trace=trace)
    np.random.seed(7)
    b = RLScheduler()
    b.train(_sim(b), episodes=30, trace=trace)
    assert a.q_table == b.q_table
```

- [ ] **Step 2: Run to verify they fail**

Run: `python -m pytest tests/scheduler/test_rl_policy.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'moesim.scheduler.policies.rl'`

- [ ] **Step 3: Implement rl.py**

```python
"""RL scheduling policy: Q-learning over expert placement actions (numpy-only)."""
from __future__ import annotations

import numpy as np

from moesim.scheduler.base import Action, Scheduler
from moesim.scheduler.state import ScheduleState


class RLScheduler(Scheduler):
    """Learn expert placement via Q-learning. Actions: load (GPU) vs execute_cpu.
    Resident experts always execute on GPU. Deterministic with fixed numpy seed."""

    def __init__(self, alpha: float = 0.1, gamma: float = 0.9, epsilon: float = 0.1) -> None:
        self.alpha = alpha
        self.gamma = gamma
        self.epsilon = epsilon
        self.q_table: dict[tuple[str, str], float] = {}

    def _q(self, key: tuple[str, str]) -> float:
        return self.q_table.get(key, 0.0)

    def _choose_action(self, expert_id: str) -> str:
        if np.random.rand() < self.epsilon:
            return "execute_cpu" if np.random.rand() < 0.5 else "load"
        q_load = self._q((expert_id, "load"))
        q_cpu = self._q((expert_id, "execute_cpu"))
        return "load" if q_load >= q_cpu else "execute_cpu"

    def decide(self, state: ScheduleState, clock: float) -> list[Action]:
        actions: list[Action] = []
        for eid in state.requested:
            if eid in state.resident:
                actions.append(Action(kind="execute_gpu", expert_ids=(eid,)))
            else:
                action = self._choose_action(eid)
                actions.append(Action(kind=action, expert_ids=(eid,)))
        return actions

    def train(self, sim, episodes: int = 200, trace: list[list[str]] | None = None,
              epsilon_decay: float = 0.99) -> None:
        trace = trace or [["e0", "e1"]]
        for episode in range(episodes):
            prev_state = None
            for step in trace:
                sim._state.requested = tuple(step)
                actions = self.decide(sim._state, sim._clock)
                # reward proxy: negative of executed cpu_ms (cheaper placement rewarded)
                reward = -sum(
                    sim.profiles[eid].cpu_exec_ms
                    for a in actions if a.kind == "execute_cpu" for eid in a.expert_ids
                )
                for a in actions:
                    if a.kind in ("load", "execute_cpu"):
                        eid = a.expert_ids[0]
                        key = (eid, a.kind)
                        q = self._q(key)
                        self.q_table[key] = q + self.alpha * (reward - q)
                prev_state = step
            self.epsilon *= epsilon_decay
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `python -m pytest tests/scheduler/test_rl_policy.py -v`
Expected: PASS

- [ ] **Step 5: Full suite + commit**

Run: `python -m pytest tests/ -q`
Expected: PASS（66 + 3 new = 69）

```bash
git add moesim/scheduler/policies/rl.py tests/scheduler/test_rl_policy.py
git commit -m "feat: numpy-only Q-learning scheduling policy trained in simulator"
```

---

### Task 28: 多 GPU 拓扑模拟扩展

**Files:**
- Modify: `moesim/sim/resources.py`（多 GPU 节点）
- Modify: `moesim/scheduler/state.py`（per-GPU 驻留）
- Test: `tests/sim/test_multi_gpu.py` (new)

**Interfaces:**
- Consumes: `ScheduleState`、`BandwidthResource`、`ExpertProfile`
- Produces:
  - `resources.py`: `MultiGPUCluster` — 节点列表（每节点 `gpu_capacity_mb`），`gpu_bandwidth_matrix: np.ndarray`（节点间带宽 GB/s）；`transfer_time_ms(src, dst, size_mb)`。
  - `state.py`: `ScheduleState` 新增 `gpu_resident: dict[int, set[str]]`（per-GPU 驻留专家）；`resident` 保持为 `gpu_resident[0]` 的视图（向后兼容旧测试）。
  - 测试：per-GPU 驻留正确；跨节点传输时间按带宽矩阵计算；旧测试不破坏。

- [ ] **Step 1: Write failing tests**（tests/sim/test_multi_gpu.py）

```python
import numpy as np

from moesim.sim.resources import MultiGPUCluster


def test_multi_gpu_transfer_time_uses_bandwidth_matrix():
    cluster = MultiGPUCluster(
        gpu_capacities_mb=[100.0, 100.0],
        bandwidth_matrix=np.array([[100.0, 20.0], [20.0, 100.0]]),
    )
    # 10MB across 20 GB/s = 0.5ms
    assert cluster.transfer_time_ms(0, 1, 10.0) == 0.5
    # same-GPU transfer negligible
    assert cluster.transfer_time_ms(0, 0, 10.0) == 0.0


def test_state_per_gpu_residency():
    from moesim.scheduler.cost_model import ExpertProfile
    from moesim.scheduler.state import ScheduleState

    profiles = {f"e{i}": ExpertProfile(f"e{i}", 1.0, 0.1, 0.5) for i in range(3)}
    s = ScheduleState(profiles=profiles, resident=set(), gpu_capacity_mb=10.0,
                      gpu_resident={0: {"e0"}, 1: {"e1"}})
    assert s.gpu_resident[0] == {"e0"}
    assert s.gpu_resident[1] == {"e1"}
    assert s.resident == {"e0"}  # view of gpu 0
```

- [ ] **Step 2: Run to verify they fail**

Run: `python -m pytest tests/sim/test_multi_gpu.py -v`
Expected: FAIL — `ImportError: cannot import name 'MultiGPUCluster'`

- [ ] **Step 3: Implement**

```python
# moesim/sim/resources.py — append
import numpy as np


class MultiGPUCluster:
    """Multiple GPU nodes with a pairwise bandwidth matrix (GB/s)."""

    def __init__(self, gpu_capacities_mb: list[float], bandwidth_matrix: np.ndarray) -> None:
        assert bandwidth_matrix.shape == (len(gpu_capacities_mb),) * 2
        self.gpu_capacities_mb = list(gpu_capacities_mb)
        self.bandwidth_matrix = bandwidth_matrix

    def transfer_time_ms(self, src: int, dst: int, size_mb: float) -> float:
        if src == dst:
            return 0.0
        return size_mb / float(self.bandwidth_matrix[src, dst])
```

```python
# moesim/scheduler/state.py — add field with default
    gpu_resident: dict[int, set[str]] = field(default_factory=lambda: {0: set()})
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `python -m pytest tests/sim/test_multi_gpu.py -v`
Expected: PASS

- [ ] **Step 5: Full suite + commit**

Run: `python -m pytest tests/ -q`
Expected: PASS（69 + 2 new = 71）

```bash
git add moesim/sim/resources.py moesim/scheduler/state.py tests/sim/test_multi_gpu.py
git commit -m "feat: multi-GPU cluster topology with per-node residency"
```

---

### Task 29: 收尾——文档、全量测试、同步 ai-scheduler、PR

**Files:**
- Modify: `README.md`（v3 交付）
- Modify: `docs/superpowers/specs/2026-08-09-moesim-design.md`（§8 边界更新）

- [ ] **Step 1: 更新 README v3 交付小节**

追加：forward hook 真实推理闭环（OLMoE 验证结果）、RL 策略、多 GPU 拓扑。

- [ ] **Step 2: 更新设计文档 §8**

v3 项移入已交付；剩余：真实 vLLM kernel 级接入、生产级 RL 训练、多 GPU 真机验证。

- [ ] **Step 3: 全量测试 + no-torch 路径**

Run: `python -m pytest tests/ -q` → PASS
Run: `python -c "from moesim.sim.sweep import compare_policies; print('ok')"` → ok

- [ ] **Step 4: Commit + 同步 ai-shced + PR**

```bash
git add README.md docs/superpowers/specs/2026-08-09-moesim-design.md
git commit -m "docs: mark v3 deliverables complete"
# sync to ai-shced feat/moesim-v3, push, open PR #2 (base main)
```

---

## Self-Review Notes

- **Spec coverage:** §4.1 forward hook → Task 25/26；§4.2 RL → Task 27；§4.3 多 GPU → Task 28；§5 验证 → 各任务测试；§6 边界 → Task 29。
- **Deliberate simplifications:** hook 仅支持 OLMoE 结构（v3 目标模型）；RL 用 Q-learning 简化（非 DQN，numpy-only）；多 GPU 仅模拟层（真机单卡）。
- **Type consistency:** `MoEForwardHook(executor, scheduler, profiles, pcie, device)` 与 v2 executor 接口一致；`ScheduleState.gpu_resident` 默认值保证旧构造调用不破坏；`RLScheduler.decide` 输出沿用 `Action(kind, expert_ids)`。
- **Determinism:** RL 用固定 numpy seed 可复现；hook 专家执行顺序不影响求和结果（浮点容差内）。
- **Regression guard:** 每个任务含 full suite 运行；既有 63 测试不破坏。
- **No placeholders:** 每个任务含完整测试代码、实现代码、精确命令与预期输出。
