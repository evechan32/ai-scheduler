# moesim v2 — 执行闭环与扩展 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成 moesim v2 执行闭环：真实权重 offload（调度决策真正驱动权重搬移）、KV 分层调度策略、INT8 量化 CPU 算子、vllm/llama.cpp 后端适配，并产出真机端到端验证协议。

**Architecture:** 在现有 v1（sim/ 模拟器 + scheduler/ 策略 + executor/ 抽象）之上补齐执行层真实能力。所有扩展保持 TDD：先写失败测试，再实现。sim/scheduler 仍 numpy-only；executor 的 torch 依赖已在 v1 约定。

**Tech Stack:** Python>=3.10, numpy>=1.24, torch>=2.1（executor 层）, transformers>=4.40（后端适配）。不新增硬依赖：offload 用 torch 原生 `.to()`；vllm/llama.cpp 适配用 optional import + mock 测试。

## Global Constraints

- Python >= 3.10。`sim/`、`scheduler/` 保持 numpy-only（禁止 torch import）。
- 确定性：同一输入两次运行输出逐位相同（无随机、无时间依赖）。
- 单位：大小 MB、带宽 GB/s、时间 ms。
- TDD：每个任务先写失败测试 → 验证失败 → 最小实现 → 验证通过 → 提交（conventional commit）。
- 不引入新的硬性依赖（不进 `dependencies`）；可选依赖进 `[project.optional-dependencies]`。
- 不删除、不修改现有通过的测试；现有 41 个测试必须保持通过。
- 本机验证环境：8G GPU（RTX 5070 Laptop）+ 7.6G 内存。Qwen3-30B-A3B（~20G+）真机端到端在本机 **blocked**（硬件不足）——只写验证协议，标记 `[blocked]`。

---

### Task 16: `executor/backends/transformers.py` — 真实权重 offload（load/unload 真实现）

**Files:**
- Modify: `moesim/executor/backends/transformers.py`
- Test: `tests/executor/test_transformers_offload.py` (new)

**Interfaces:**
- Consumes: `ExpertExecutor` (base.py: `load(expert_ids: list[str])`, `unload(expert_ids: list[str])`, `execute_gpu(expert_id: str, hidden_states) -> object`, `execute_cpu(expert_id: str, hidden_states) -> object`)
- Produces: `TransformersMoEExecutor` 的 load/unload 从 v1 no-op 改为真实搬移：`load` 把专家模块参数 `.to("cuda")`，`unload` 把参数 `.to("cpu")`。新增 `residency: dict[str, str]` 记录每个专家所在设备。`execute_gpu` 前自动 load（若在 CPU 上）；`execute_cpu` 自动把权重送到 CPU 执行。
- 约束：模型由 `nn.Module` 构造，专家是 `model.experts[i]`，模块含 `w1.weight`/`w2.weight`（与 v1 mini-MoE 测试一致）。

- [ ] **Step 1: Write the failing test**

```python
# tests/executor/test_transformers_offload.py
import pytest

torch = pytest.importorskip("torch")

from moesim.executor.backends.transformers import TransformersMoEExecutor  # noqa: E402


def _make_model():
    import torch.nn as nn

    class MiniExpert(nn.Module):
        def __init__(self):
            super().__init__()
            self.w1 = nn.Linear(8, 16, bias=False)
            self.w2 = nn.Linear(16, 8, bias=False)

        def forward(self, x):
            return self.w2(nn.functional.gelu(self.w1(x)))

    model = nn.Module()
    model.experts = nn.ModuleList([MiniExpert(), MiniExpert()])
    return model


def test_load_moves_weights_to_gpu():
    torch.cuda.is_available() or pytest.skip("no CUDA")
    ex = TransformersMoEExecutor(_make_model())
    ex.load(["0"])
    assert ex.model.experts[0].w1.weight.device.type == "cuda"
    assert ex.residency["0"] == "cuda"


def test_unload_moves_weights_back_to_cpu():
    torch.cuda.is_available() or pytest.skip("no CUDA")
    ex = TransformersMoEExecutor(_make_model())
    ex.load(["0"])
    ex.unload(["0"])
    assert ex.model.experts[0].w1.weight.device.type == "cpu"
    assert ex.residency["0"] == "cpu"


def test_execute_gpu_auto_loads_and_output_on_gpu():
    torch.cuda.is_available() or pytest.skip("no CUDA")
    ex = TransformersMoEExecutor(_make_model())
    x = torch.randn(2, 8)
    out = ex.execute_gpu("1", x)
    assert out.device.type == "cuda"
    assert ex.residency["1"] == "cuda"


def test_execute_cpu_runs_on_cpu_and_output_on_cpu():
    ex = TransformersMoEExecutor(_make_model())
    x = torch.randn(2, 8)
    out = ex.execute_cpu("0", x)
    assert out.device.type == "cpu"
    assert ex.residency["0"] == "cpu"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/executor/test_transformers_offload.py -v`
Expected: FAIL — `AttributeError: 'TransformersMoEExecutor' object has no attribute 'residency'`（load 仍是 no-op，权重仍在 CPU）

- [ ] **Step 3: Implement — rewrite transformers.py**

```python
"""Transformers backend adapter: route expert FFNs through the executor abstraction."""
from __future__ import annotations

from moesim.executor.base import ExpertExecutor
from moesim.executor.cpu_kernels import expert_ffn


class TransformersMoEExecutor(ExpertExecutor):
    """Wraps a HF-style MoE model (model.experts: ModuleList of FFN modules).

    load/unload move expert parameters between GPU and CPU for real
    (v2: weight offloading). execute_gpu auto-loads; execute_cpu runs the
    weights on CPU via the moesim CPU kernel.
    """

    def __init__(self, model, device: str = "cuda") -> None:
        self.model = model
        self.device = device
        self.residency: dict[str, str] = {}

    def _expert_module(self, expert_id: str):
        return self.model.experts[int(expert_id)]

    def _to(self, module, target: str) -> None:
        module.to(target)

    def load(self, expert_ids: list[str]) -> None:
        for eid in expert_ids:
            self._to(self._expert_module(eid), self.device)
            self.residency[eid] = self.device

    def unload(self, expert_ids: list[str]) -> None:
        for eid in expert_ids:
            self._to(self._expert_module(eid), "cpu")
            self.residency[eid] = "cpu"

    def execute_gpu(self, expert_id: str, hidden_states) -> object:
        if self.residency.get(expert_id) != self.device:
            self.load([expert_id])
        return self._expert_module(expert_id)(hidden_states.to(self.device))

    def execute_cpu(self, expert_id: str, hidden_states) -> object:
        module = self._expert_module(expert_id)
        if self.residency.get(expert_id) != "cpu":
            self.unload([expert_id])
        w1 = module.w1.weight.detach()
        w2 = module.w2.weight.detach()
        return expert_ffn(hidden_states, w1, w2)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/executor/test_transformers_offload.py -v`
Expected: 4 passed（无 CUDA 环境则显示 4 skipped，仍视为通过）

- [ ] **Step 5: Run full suite to confirm no regression**

Run: `python -m pytest tests/ -q`
Expected: 45 passed（原 41 + 新 4；无 CUDA 时为 41 passed, 4 skipped）

- [ ] **Step 6: Commit**

```bash
git add moesim/executor/backends/transformers.py tests/executor/test_transformers_offload.py
git commit -m "feat: real weight offload in transformers executor (load/unload move params)"
```

---

### Task 17: KV cache 分层调度策略（完整联合调度）

**Files:**
- Modify: `moesim/executor/kv_manager.py`
- Modify: `moesim/scheduler/state.py`
- Create: `moesim/scheduler/policies/kv_aware.py`
- Test: `tests/executor/test_kv_manager.py` (modify — add new tests)
- Test: `tests/scheduler/test_kv_aware_policy.py` (new)

**Interfaces:**
- Consumes: `ScheduleState` (state.py), `ExpertProfile` (cost_model.py), `Action`/`Scheduler` (base.py), `KVTierManager` (kv_manager.py)
- Produces:
  - `KVTierManager` 增强：新增 `gpu_used_mb`/`host_used_mb` 之外的 `total_kv_mb()`、`pressure()`（used/total 比例）；`transfer_host_to_gpu(mb)`（与既有 `transfer_gpu_to_host` 对称）；`evict_to_host(mb)` 别名。
  - `ScheduleState` 新增字段：`kv_gpu_mb: float = 0.0`、`kv_host_mb: float = 0.0`、`kv_gpu_capacity_mb: float = 0.0`（dataclass 默认值，不破坏既有构造调用）。
  - `KVWeightedPolicy(Scheduler)`：在 CostModelPolicy 基础上叠加 KV 压力感知——当 GPU KV 池压力 > 0.9 时，优先选择 CPU 执行专家（减少 GPU 驻留），并产生 `Action(kind="evict_kv", expert_ids=(eid,))` 驱逐冷 KV；KV 压力低时行为等同 CostModelPolicy。

- [ ] **Step 1: Extend KVTierManager with missing methods (TDD test first)**

```python
# tests/executor/test_kv_manager.py (append)
def test_transfer_host_to_gpu_and_total():
    m = KVTierManager(gpu_pool_mb=100.0, host_pool_mb=200.0)
    m.allocate_gpu(30.0)
    m.transfer_gpu_to_host(10.0)
    assert m.gpu_used_mb == pytest.approx(20.0)
    assert m.host_used_mb == pytest.approx(10.0)
    assert m.total_kv_mb() == pytest.approx(30.0)
    m.transfer_host_to_gpu(5.0)
    assert m.gpu_used_mb == pytest.approx(25.0)
    assert m.host_used_mb == pytest.approx(5.0)


def test_pressure_ratio():
    m = KVTierManager(gpu_pool_mb=100.0, host_pool_mb=200.0)
    assert m.pressure() == pytest.approx(0.0)
    m.allocate_gpu(90.0)
    assert m.pressure() == pytest.approx(0.9)
```

- [ ] **Step 2: Run to verify it fails**

Run: `python -m pytest tests/executor/test_kv_manager.py -v`
Expected: FAIL — `AttributeError: 'KVTierManager' object has no attribute 'total_kv_mb'`

- [ ] **Step 3: Implement KVTierManager extensions**

```python
# moesim/executor/kv_manager.py — add to class
    def total_kv_mb(self) -> float:
        return self.gpu_used_mb + self.host_used_mb

    def pressure(self) -> float:
        return self.total_kv_mb() / (self.gpu_pool_mb + self.host_pool_mb)

    def transfer_host_to_gpu(self, mb: float) -> None:
        if mb > self.host_used_mb + 1e-9:
            raise ValueError(f"cannot transfer {mb}MB, only {self.host_used_mb}MB on host")
        self.host_used_mb -= mb
        self.gpu_used_mb += mb
```

- [ ] **Step 4: Verify KV manager tests pass**

Run: `python -m pytest tests/executor/test_kv_manager.py -v`
Expected: PASS（含新 2 个）

- [ ] **Step 5: Write failing test for KV-aware policy**

```python
# tests/scheduler/test_kv_aware_policy.py
from moesim.scheduler.cost_model import ExpertProfile
from moesim.scheduler.policies.kv_aware import KVWeightedPolicy
from moesim.scheduler.state import ScheduleState


def _profiles() -> dict[str, ExpertProfile]:
    return {
        f"e{i}": ExpertProfile(
            expert_id=f"e{i}", size_mb=10.0,
            gpu_exec_ms=1.0, cpu_exec_ms=5.0, activation_freq=1.0 - i * 0.1,
        )
        for i in range(4)
    }


def test_low_kv_pressure_behaves_like_cost_model():
    profiles = _profiles()
    state = ScheduleState(
        profiles=profiles, resident=set(), gpu_capacity_mb=1000.0,
        requested=("e0",), kv_gpu_capacity_mb=1000.0, kv_gpu_mb=10.0,
    )
    actions = KVWeightedPolicy().decide(state, 0.0)
    # GPU 空间充足、KV 压力低 → 加载到 GPU
    assert any(a.kind == "load" for a in actions)


def test_high_kv_pressure_prefers_cpu_and_evicts_kv():
    profiles = _profiles()
    state = ScheduleState(
        profiles=profiles, resident=set(), gpu_capacity_mb=1000.0,
        requested=("e0",), kv_gpu_capacity_mb=100.0, kv_gpu_mb=99.0,
        access_history=["e1", "e2", "e3"],
    )
    actions = KVWeightedPolicy().decide(state, 0.0)
    # KV 压力 0.99 → 专家放 CPU，且驱逐冷 KV
    assert any(a.kind == "execute_cpu" for a in actions)
    assert any(a.kind == "evict_kv" for a in actions)
```

- [ ] **Step 6: Run to verify it fails**

Run: `python -m pytest tests/scheduler/test_kv_aware_policy.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'moesim.scheduler.policies.kv_aware'`

- [ ] **Step 7: Extend ScheduleState with KV fields**

```python
# moesim/scheduler/state.py — add dataclass fields (defaults keep old call sites working)
    kv_gpu_mb: float = 0.0
    kv_host_mb: float = 0.0
    kv_gpu_capacity_mb: float = 0.0
```

- [ ] **Step 8: Implement KVWeightedPolicy**

```python
"""KV-pressure-aware policy: extends CostModelPolicy with KV tier pressure."""
from __future__ import annotations

from copy import deepcopy

from moesim.scheduler.base import Action
from moesim.scheduler.policies.cost_model import CostModelPolicy
from moesim.scheduler.state import ScheduleState


class KVWeightedPolicy(CostModelPolicy):
    def __init__(self, pcie=None, cpu_concurrency: int = 1,
                 prefetch_n: int = 1, kv_pressure_threshold: float = 0.9) -> None:
        super().__init__(pcie=pcie, cpu_concurrency=cpu_concurrency,
                         prefetch_n=prefetch_n)
        self.kv_pressure_threshold = kv_pressure_threshold

    def _kv_pressure(self, state: ScheduleState) -> float:
        if state.kv_gpu_capacity_mb <= 0:
            return 0.0
        return state.kv_gpu_mb / state.kv_gpu_capacity_mb

    def decide(self, state: ScheduleState, clock: float) -> list[Action]:
        actions = super().decide(state, clock)
        pressure = self._kv_pressure(state)
        if pressure <= self.kv_pressure_threshold:
            return actions

        # KV pressure high: force requested non-resident experts to CPU and evict cold KV
        sim = deepcopy(state)
        requested = list(state.requested)
        result: list[Action] = []
        for a in actions:
            if a.kind == "load" and a.expert_ids[0] in requested:
                result.append(Action(kind="execute_cpu", expert_ids=a.expert_ids))
            else:
                result.append(a)
        # evict one cold KV entry (most recently touched, non-requested)
        for eid in reversed(state.access_history):
            if eid not in requested:
                result.append(Action(kind="evict_kv", expert_ids=(eid,)))
                break
        return result
```

- [ ] **Step 9: Verify KV-aware policy tests pass**

Run: `python -m pytest tests/scheduler/test_kv_aware_policy.py -v`
Expected: PASS

- [ ] **Step 10: Full suite + commit**

Run: `python -m pytest tests/ -q`
Expected: PASS（原 41 + 新增）

```bash
git add moesim/executor/kv_manager.py moesim/scheduler/state.py \
  moesim/scheduler/policies/kv_aware.py \
  tests/executor/test_kv_manager.py tests/scheduler/test_kv_aware_policy.py
git commit -m "feat: KV tier aware scheduling policy with GPU pressure control"
```

---

### Task 18: INT8 量化 CPU expert 算子

**Files:**
- Create: `moesim/executor/cpu_kernels/quantized.py`
- Test: `tests/executor/test_quantized_kernel.py` (new)

**Interfaces:**
- Consumes: `expert_ffn` 的语义（`(hidden_states, w1, w2) -> out`，输入 fp16）
- Produces: `expert_ffn_int8(hidden_states: torch.Tensor, w1: torch.Tensor, w2: torch.Tensor, scale: float = 1.0) -> torch.Tensor` —— 把 w1/w2 量化到 int8（per-tensor scale，对称量化），CPU 上执行 int8 权重 × fp16 激活的 matmul，输出反量化回 fp16。数值误差相对 FP16 参考 < 5%（绝对阈值测试用 1e-1）。
- 说明：v2 量化先行 INT8（稳定、torch 原生支持）；INT4 留作后续扩展点（文档注明）。

- [ ] **Step 1: Write the failing test**

```python
# tests/executor/test_quantized_kernel.py
import pytest

torch = pytest.importorskip("torch")

from moesim.executor.cpu_kernels.quantized import expert_ffn_int8  # noqa: E402


def test_int8_output_shape_and_close_to_fp16():
    torch.manual_seed(0)
    x = torch.randn(2, 16, dtype=torch.float16)
    w1 = torch.randn(32, 16, dtype=torch.float16) - 0.5
    w2 = torch.randn(16, 32, dtype=torch.float16) - 0.5

    ref = torch.nn.functional.gelu(x @ w1.t()) @ w2.t()
    out = expert_ffn_int8(x, w1, w2)

    assert out.shape == ref.shape
    assert out.dtype == torch.float16
    # 相对误差 < 5%（int8 量化误差）
    rel_err = (out.float() - ref.float()).abs().mean() / ref.float().abs().mean()
    assert rel_err < 0.05, f"rel err {rel_err:.4f}"


def test_int8_deterministic():
    torch.manual_seed(1)
    x = torch.randn(2, 16, dtype=torch.float16)
    w1 = torch.randn(32, 16, dtype=torch.float16) - 0.5
    w2 = torch.randn(16, 32, dtype=torch.float16) - 0.5
    a = expert_ffn_int8(x, w1, w2)
    b = expert_ffn_int8(x, w1, w2)
    assert torch.equal(a, b)
```

- [ ] **Step 2: Run to verify it fails**

Run: `python -m pytest tests/executor/test_quantized_kernel.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'moesim.executor.cpu_kernels.quantized'`

- [ ] **Step 3: Implement quantized kernel**

```python
# moesim/executor/cpu_kernels/quantized.py
"""INT8 quantized expert FFN for CPU (v2 quantization, INT4 later)."""
from __future__ import annotations

import torch


def _quantize_symmetric(t: torch.Tensor) -> tuple[torch.Tensor, torch.Tensor]:
    """Per-tensor symmetric INT8 quantization. Returns (int8_tensor, scale)."""
    amax = t.float().abs().max()
    if amax == 0:
        return torch.zeros_like(t, dtype=torch.int8), torch.tensor(1.0)
    scale = amax / 127.0
    q = torch.clamp(torch.round(t.float() / scale), -127, 127).to(torch.int8)
    return q, scale


def expert_ffn_int8(
    hidden_states: torch.Tensor,
    w1: torch.Tensor,
    w2: torch.Tensor,
) -> torch.Tensor:
    """INT8-weight expert FFN on CPU: x -> gelu(x @ w1_t) @ w2_t with int8 weights.

    Weights are quantized per-tensor to int8; activations stay fp16; matmuls use
    fp16 activations against int8 weights cast back to fp16 (accuracy-first v2
    quantization; real int8 gemm kernels are a follow-up).
    """
    q1, s1 = _quantize_symmetric(w1)
    q2, s2 = _quantize_symmetric(w2)
    w1_r = q1.float().to(hidden_states.dtype) * s1
    w2_r = q2.float().to(hidden_states.dtype) * s2
    h = torch.nn.functional.gelu(hidden_states @ w1_r.t())
    return h @ w2_r.t()
```

- [ ] **Step 4: Verify tests pass**

Run: `python -m pytest tests/executor/test_quantized_kernel.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add moesim/executor/cpu_kernels/quantized.py tests/executor/test_quantized_kernel.py
git commit -m "feat: INT8 quantized CPU expert FFN kernel with per-tensor scale"
```

---

### Task 19: vllm / llama.cpp 后端适配

**Files:**
- Create: `moesim/executor/backends/vllm.py`
- Create: `moesim/executor/backends/llama_cpp.py`
- Test: `tests/executor/test_remote_backends.py` (new)

**Interfaces:**
- Consumes: `ExpertExecutor` (base.py)
- Produces:
  - `VLLMExecutor(ExpertExecutor)`：包装 `vllm` 引擎句柄。`execute_gpu/expert_id, hidden_states` 转发到 vLLM 的 `LLM` 推理（v1 契约：接受任意对象并透传，返回对象）；`load/unload` 记录 residency（vLLM 内部管理显存，此处仅记账）。
  - `LlamaCppExecutor(ExpertExecutor)`：包装 `llama_cpp.Llama` 实例。`execute_gpu` 调用 `llama.eval`/`llama.embed` 等价接口；`load/unload` 记账。
  - 两者均在模块顶部 optional import：vllm/llama_cpp 未安装时抛出带安装提示的 `ImportError`。
  - 测试用 fake 引擎对象（鸭子类型）验证接口契约与记账逻辑，不依赖真实 vllm/llama_cpp 安装。

- [ ] **Step 1: Write the failing test**

```python
# tests/executor/test_remote_backends.py
import pytest

from moesim.executor.backends.vllm import VLLMExecutor  # noqa: E402
from moesim.executor.backends.llama_cpp import LlamaCppExecutor  # noqa: E402


class FakeVLLM:
    def __init__(self):
        self.calls = []

    def generate(self, *args, **kwargs):
        self.calls.append(("generate", args, kwargs))
        return "output"


class FakeLlama:
    def __init__(self):
        self.calls = []

    def eval(self, *args, **kwargs):
        self.calls.append(("eval", args, kwargs))
        return [0.1, 0.2]


def test_vllm_executor_records_residency_and_forwards():
    engine = FakeVLLM()
    ex = VLLMExecutor(engine)
    ex.load(["e0"])
    assert ex.residency["e0"] == "loaded"
    out = ex.execute_gpu("e0", object())
    assert out == "output"
    assert engine.calls[-1][0] == "generate"
    ex.unload(["e0"])
    assert ex.residency["e0"] == "unloaded"


def test_llamacpp_executor_forwards_to_eval():
    engine = FakeLlama()
    ex = LlamaCppExecutor(engine)
    ex.load(["e1"])
    assert ex.residency["e1"] == "loaded"
    out = ex.execute_gpu("e1", [1, 2, 3])
    assert out == [0.1, 0.2]
    assert engine.calls[-1][0] == "eval"
```

- [ ] **Step 2: Run to verify it fails**

Run: `python -m pytest tests/executor/test_remote_backends.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'moesim.executor.backends.vllm'`

- [ ] **Step 3: Implement vllm.py**

```python
"""vLLM executor backend — forwards expert execution to a vLLM engine handle."""
from __future__ import annotations

from moesim.executor.base import ExpertExecutor


class VLLMExecutor(ExpertExecutor):
    """Wrap a vLLM engine. load/unload only record residency (vLLM manages its
    own device memory); execute_gpu forwards to the engine's generate().

    Requires vllm installed: `pip install vllm`. Import fails with a clear
    message otherwise.
    """

    def __init__(self, engine) -> None:
        try:
            import vllm  # noqa: F401
        except ImportError as exc:  # pragma: no cover - env-dependent
            raise ImportError(
                "VLLMExecutor requires vllm: pip install vllm"
            ) from exc
        self.engine = engine
        self.residency: dict[str, str] = {}

    def load(self, expert_ids: list[str]) -> None:
        for eid in expert_ids:
            self.residency[eid] = "loaded"

    def unload(self, expert_ids: list[str]) -> None:
        for eid in expert_ids:
            self.residency[eid] = "unloaded"

    def execute_gpu(self, expert_id: str, hidden_states) -> object:
        return self.engine.generate(hidden_states)

    def execute_cpu(self, expert_id: str, hidden_states) -> object:
        raise NotImplementedError("vLLM backend executes on GPU only")
```

- [ ] **Step 4: Implement llama_cpp.py**

```python
"""llama.cpp executor backend — forwards expert execution to a llama_cpp handle."""
from __future__ import annotations

from moesim.executor.base import ExpertExecutor


class LlamaCppExecutor(ExpertExecutor):
    """Wrap a llama-cpp-python Llama instance. load/unload record residency;
    execute_gpu forwards to eval().

    Requires llama-cpp-python: `pip install llama-cpp-python`.
    """

    def __init__(self, llama) -> None:
        try:
            import llama_cpp  # noqa: F401
        except ImportError as exc:  # pragma: no cover - env-dependent
            raise ImportError(
                "LlamaCppExecutor requires llama-cpp-python: pip install llama-cpp-python"
            ) from exc
        self.llama = llama
        self.residency: dict[str, str] = {}

    def load(self, expert_ids: list[str]) -> None:
        for eid in expert_ids:
            self.residency[eid] = "loaded"

    def unload(self, expert_ids: list[str]) -> None:
        for eid in expert_ids:
            self.residency[eid] = "unloaded"

    def execute_gpu(self, expert_id: str, hidden_states) -> object:
        return self.llama.eval(hidden_states)

    def execute_cpu(self, expert_id: str, hidden_states) -> object:
        raise NotImplementedError("llama.cpp backend executes on GPU only")
```

- [ ] **Step 5: Verify tests pass**

Run: `python -m pytest tests/executor/test_remote_backends.py -v`
Expected: PASS（fake 引擎 + optional import 已满足）

- [ ] **Step 6: Full suite + commit**

Run: `python -m pytest tests/ -q`
Expected: PASS

```bash
git add moesim/executor/backends/vllm.py moesim/executor/backends/llama_cpp.py \
  tests/executor/test_remote_backends.py
git commit -m "feat: vllm and llama.cpp executor backends with optional imports"
```

---

### Task 20: 真机端到端验证（Qwen3-4B-A2B 小模型，本机可执行）

**Files:**
- Create: `benchmarks/e2e/README.md`（modify — 追加真实机型验证协议）
- Create: `benchmarks/e2e/verify_on_real_machine.md` (new)

**Interfaces:**
- Consumes: `benchmarks/microbench/`（真实测量）、`benchmarks/e2e/compare_moesim_vs_llamacpp.py`、`executor/backends/`（Task 16/19）
- Produces: 一份可执行的验证协议文档：在真机上验证模拟误差 < 20% 的完整步骤。

**模型方案（已确认，Qwen3-4B-A2B 不存在，改用 OLMoE）**：OLMoE-1B-7B-0125（MoE：7B 总参 / 1.3B 激活 / 64 专家 / 8 激活），GGUF Q4_K_M ~4.2GB，本机 8G GPU + 7.6G 内存可运行。
- 下载（ModelScope 国内可达）：
  - 原始权重（transformers）：`modelscope download --model allenai/OLMoE-1B-7B-0125 --local_dir ./models/olmoe-1b-7b`
  - GGUF（llama.cpp）：`modelscope download --model allenai/OLMoE-1B-7B-0125-GGUF --local_dir ./models/olmoe-1b-7b-gguf`
- 说明：Qwen3-30B-A3B 方案保留在协议文档作为"目标机型（12G/16G）验证"，本机用 OLMoE-1B-7B 完成闭环。

- [ ] **Step 1: Write the verification protocol document**

`benchmarks/e2e/verify_on_real_machine.md` 内容覆盖（markdown，无占位符，全部写实际命令）：
1. 前置：12G GPU + 16G+ RAM 真机；安装 moesim（pip install -e .）、llama.cpp（含 `--n-cpu-moe`）、可选 MoE-Infinity；下载 Qwen3-30B-A3B GGUF（`--n-cpu-moe 8` 实测）。
2. 校准：跑 `python benchmarks/microbench/measure_pcie.py` 与 `measure_expert_time.py`（真实 expert 尺寸/时间），产出 `out/pcie.json`、`out/profiles.json`。
3. 模拟：`python benchmarks/e2e/compare_moesim_vs_llamacpp.py --profiles out/profiles.json --pcie out/pcie.json` → 记录三种策略模拟 TPOT。
4. 实测基线：`llama-bench -m <gguf> --n-cpu-moe 8 --cpu-moe` → 记录真实 TPOT。
5. 对比：模拟 TPOT vs 实测 TPOT 相对误差 = |sim - real| / real；**判定：< 20% 通过**。
6. 填表模板（与现有 e2e README 表格一致）。
7. 明确标注：本协议设计目标机型为 12G/16G，开发机（8G/7.6G）无法运行 Qwen3-30B-A3B，属于硬件受限 [blocked]，代码与脚本均已就绪，待真机执行。

- [ ] **Step 2: Commit**

```bash
git add benchmarks/e2e/verify_on_real_machine.md
git commit -m "docs: real-machine verification protocol for Qwen3-30B-A3B (hardware blocked on dev machine)"
```

---

### Task 21: 收尾——文档、full suite、同步 ai-scheduler

**Files:**
- Modify: `README.md`（v2 进度更新：offload 真实化、KV 策略、INT8 kernel、后端适配）
- Modify: `docs/superpowers/specs/2026-08-09-moesim-design.md`（§8 交付边界更新：已完成项移动至 v1.5 已交付）

**Interfaces:**
- Consumes: Task 16-20 全部产出。

- [ ] **Step 1: Update README v2 状态**

在 README "Roadmap (v2)" 段落后追加 "v2 已交付" 小节，列出：真实权重 offload（Task 16）、KV 感知调度策略（Task 17）、INT8 量化 kernel（Task 18）、vllm/llama.cpp 后端（Task 19）、真机验证协议（Task 20，标记 [blocked: 需 12G/16G 真机]）。

- [ ] **Step 2: Update design doc §8**

把已完成的 v2 项从 "v2（扩展点）" 移到新的 "v1.5 已交付" 段；剩余 v2：INT4 量化、多 GPU/集群拓扑、RL 策略、vllm 真实引擎联调。

- [ ] **Step 3: Full suite + no-torch path verification**

Run: `python -m pytest tests/ -q`
Expected: PASS

Run: `python -c "from moesim.sim.sweep import compare_policies; print('ok')"`（numpy-only 路径）
Expected: prints ok

- [ ] **Step 4: Commit + sync to ai-scheduler**

```bash
git add README.md docs/superpowers/specs/2026-08-09-moesim-design.md
git commit -m "docs: mark v2 deliverables complete; update spec §8 boundaries"
# sync to ai-shced repo (same procedure as v1)
```

---

## Self-Review Notes

- **Spec coverage:** §5.3 executor（offload 真实化 Task 16、量化 Task 18、vllm/llama_cpp 后端 Task 19）；§5.3 kv_manager 完整实现（Task 17）；§5.4 e2e 真机协议（Task 20）；§8 v2 边界更新（Task 21）。
- **Deliberate simplifications:** INT8 先行、INT4 留扩展点（Task 18 注明）；offload 用 torch 原生 `.to()` 而非 accelerate/device_map（不加硬依赖，行为等价，文档注明 device_map 为进阶用法）；KV 策略基于 ScheduleState 记账而非真实显存指针（确定性优先）。
- **Determinism:** 所有新策略/算子无随机（INT8 量化确定）；测试固定 seed。
- **Type consistency:** `expert_ffn_int8(hidden_states, w1, w2)` 与 v1 `expert_ffn` 同签名模式；`Action(kind="evict_kv", expert_ids=(eid,))` 沿用既有 Action 构造；`ScheduleState` 新字段全部带默认值，旧调用点（`ScheduleState(profiles=, resident=, gpu_capacity_mb=)`）不受影响。
- **No placeholders:** 每个任务含完整测试代码、实现代码、精确命令与预期输出。
- **Regression guard:** 每个任务 Step 均包含 full suite 运行；不改动既有测试断言。

---

### Task 22: 模拟器消费 KV 动作（evict_kv/fetch_kv 真实生效）

**Files:**
- Modify: `moesim/scheduler/base.py`（apply_actions 处理 evict_kv/fetch_kv）
- Modify: `moesim/sim/moe_adapter.py`（KV 驱逐触发 PCIe 传输计时）
- Test: `tests/scheduler/test_apply_actions_kv.py` (new)
- Test: `tests/sim/test_kv_simulation.py` (new)

**Interfaces:**
- Consumes: `Action(kind="evict_kv"/"fetch_kv", expert_ids=(eid,))`、`ScheduleState.kv_gpu_mb/kv_host_mb`、`BandwidthResource`
- Produces:
  - `apply_actions`: evict_kv 把 `kv_gpu_mb` 减少（按专家 size_mb 计），转移给 `kv_host_mb`；fetch_kv 反向。
  - `MoESimulation._step`: 收集 evict_kv 动作，通过 pcie.reserve 模拟 GPU→主机传输并推进时钟；fetch_kv 同理反向。
  - KV 驱逐/回取计入 step 时间（与专家 load 并列）。

- [ ] **Step 1: Write failing test for apply_actions KV**

```python
# tests/scheduler/test_apply_actions_kv.py
from moesim.scheduler.base import Action, apply_actions
from moesim.scheduler.cost_model import ExpertProfile
from moesim.scheduler.state import ScheduleState


def _state() -> ScheduleState:
    profiles = {f"e{i}": ExpertProfile(expert_id=f"e{i}", size_mb=10.0,
                 gpu_exec_ms=1.0, cpu_exec_ms=5.0) for i in range(3)}
    return ScheduleState(profiles=profiles, resident=set(),
                         gpu_capacity_mb=100.0, kv_gpu_mb=30.0, kv_host_mb=0.0)


def test_evict_kv_moves_gpu_to_host():
    s = _state()
    apply_actions(s, [Action(kind="evict_kv", expert_ids=("e0",))])
    assert s.kv_gpu_mb == 20.0
    assert s.kv_host_mb == 10.0


def test_fetch_kv_moves_host_to_gpu():
    s = _state()
    s.kv_gpu_mb = 0.0
    s.kv_host_mb = 30.0
    apply_actions(s, [Action(kind="fetch_kv", expert_ids=("e0",))])
    assert s.kv_gpu_mb == 10.0
    assert s.kv_host_mb == 20.0
```

- [ ] **Step 2: Run to verify it fails**

Run: `python -m pytest tests/scheduler/test_apply_actions_kv.py -v`
Expected: FAIL — kv_gpu_mb 不变（apply_actions 未处理 evict_kv）

- [ ] **Step 3: Implement apply_actions KV handling**

```python
# moesim/scheduler/base.py — add before the informational comment
    for action in actions:
        if action.kind == "evict_kv":
            for eid in action.expert_ids:
                size = state.profiles[eid].size_mb
                evict = min(size, state.kv_gpu_mb)
                state.kv_gpu_mb -= evict
                state.kv_host_mb += evict
    for action in actions:
        if action.kind == "fetch_kv":
            for eid in action.expert_ids:
                size = state.profiles[eid].size_mb
                fetch = min(size, state.kv_host_mb)
                state.kv_host_mb -= fetch
                state.kv_gpu_mb += fetch
```

- [ ] **Step 4: Verify KV apply_actions tests pass**

Run: `python -m pytest tests/scheduler/test_apply_actions_kv.py -v`
Expected: PASS

- [ ] **Step 5: Write failing test for simulator KV timing**

```python
# tests/sim/test_kv_simulation.py
from moesim.scheduler.cost_model import ExpertProfile
from moesim.scheduler.policies.lru import LRUPolicy
from moesim.scheduler.policies.kv_aware import KVWeightedPolicy
from moesim.scheduler.state import ScheduleState
from moesim.sim.moe_adapter import MoESimulation
from moesim.sim.resources import BandwidthResource, ComputeResource


def _profiles() -> dict[str, ExpertProfile]:
    return {f"e{i}": ExpertProfile(expert_id=f"e{i}", size_mb=10.0,
            gpu_exec_ms=1.0, cpu_exec_ms=5.0) for i in range(4)}


def test_kv_policy_evict_increases_sim_time():
    """Under KV pressure, evict_kv triggers a PCIe transfer that adds to step time."""
    profiles = _profiles()
    # KV pressure 0.99 -> KVWeightedPolicy evicts; host transfer 10MB @ 10GB/s = 0.01ms
    pcie = BandwidthResource(bandwidth_gbps=10.0)
    sim = MoESimulation(
        scheduler=KVWeightedPolicy(pcie=pcie),
        profiles=profiles,
        gpu_capacity_mb=1000.0,
        pcie=pcie,
        gpu=ComputeResource(concurrency=1, per_unit_ms=1.0),
        cpu=ComputeResource(concurrency=4, per_unit_ms=1.0),
    )
    # drive state to high KV pressure manually via the exposed state
    sim._state.kv_gpu_capacity_mb = 100.0
    sim._state.kv_gpu_mb = 99.0
    sim._state.access_history = ["e1", "e2", "e3"]
    metrics = sim.run([["e0"]])
    # evict_kv of 10MB @ 10GB/s = 0.01ms should appear in step time
    assert metrics.tpot_ms() > 1.0  # gpu_exec 1ms + kv evict transfer 0.01ms
```

- [ ] **Step 6: Run to verify it fails**

Run: `python -m pytest tests/sim/test_kv_simulation.py -v`
Expected: FAIL — tpot == 1.0（evict_kv 未计入时间）

- [ ] **Step 7: Implement simulator KV timing**

```python
# moesim/sim/moe_adapter.py — in _step, after load scheduling, add KV transfers
        # KV evict/fetch transfers run over PCIe concurrently with expert loads.
        kv_times: list[float] = []
        for action in actions:
            if action.kind == "evict_kv":
                for eid in action.expert_ids:
                    kv_times.append(self.pcie.reserve(self._clock, self.profiles[eid].size_mb))
            elif action.kind == "fetch_kv":
                for eid in action.expert_ids:
                    kv_times.append(self.pcie.reserve(self._clock, self.profiles[eid].size_mb))
        # include KV transfer completions in the step's completion time
        completions.extend(kv_times)
```

- [ ] **Step 8: Verify simulator KV test passes**

Run: `python -m pytest tests/sim/test_kv_simulation.py -v`
Expected: PASS

- [ ] **Step 9: Full suite + commit**

Run: `python -m pytest tests/ -q`
Expected: PASS（53 + 4 new）

```bash
git add moesim/scheduler/base.py moesim/sim/moe_adapter.py \
  tests/scheduler/test_apply_actions_kv.py tests/sim/test_kv_simulation.py
git commit -m "feat: simulator consumes KV evict/fetch actions with PCIe timing"
```
